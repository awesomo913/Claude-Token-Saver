# From: universal_client.py:278
# Ignored — browser AI uses whatever model is loaded in the UI.

    def update_settings(
        self,
        model_name: Optional[str] = None,
        max_tokens: Optional[int] = None,
        temperature: Optional[float] = None,
    ) -> None:
        """Ignored — browser AI uses whatever model is loaded in the UI."""
        pass
